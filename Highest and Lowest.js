//function highAndLow(numbers){let num = numbers.split(' ')return `${Math.max(...num)} ${Math.min(...num)}`;}

function highAndLow(numbers){
    let num = numbers.split(' ')
    return `${Math.max(...num)} ${Math.min(...num)}`;

}
